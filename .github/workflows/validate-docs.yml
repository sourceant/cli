name: Validate Docs

on:
  push:
    paths: ['docs/**', 'docs.json', '.github/workflows/validate-docs.yml']
  pull_request:
    paths: ['docs/**', 'docs.json', '.github/workflows/validate-docs.yml']

permissions:
  contents: read

jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: whilesmart/docs-kit/.github/actions/validate-docs@b9029c4db050c27d086f0735038c5677845f313c
